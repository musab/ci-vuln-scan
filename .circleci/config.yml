version: 2.1

jobs:
  vuln-scan:
    docker:
      - image: cimg/python:3.10.4
    steps:
      - run:
          name: install whispers
          command: |
            git clone https://github.com/Skyscanner/whispers
            cd whispers
            make install
      - run:
          name: run whispers
          command: whispers --help

workflows:
  vuln-scan-workflow:
    jobs:
      - vuln-scan
